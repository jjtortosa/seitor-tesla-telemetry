# Tesla Fleet Telemetry Server Configuration
# Copy this file to .env and update the values

# =============================================================================
# Domain Configuration
# =============================================================================
# Your public domain for Tesla Fleet Telemetry
# Must have valid DNS pointing to this server
TELEMETRY_DOMAIN=tesla-telemetry.example.com

# =============================================================================
# MQTT Configuration
# =============================================================================
# Host where MQTT broker (Mosquitto) is running
# Use your Home Assistant IP if using Mosquitto add-on
MQTT_HOST=192.168.1.100
MQTT_PORT=1883

# MQTT credentials (leave empty if no auth required)
MQTT_USERNAME=
MQTT_PASSWORD=

# Topic base for Tesla telemetry messages
# Messages will be published to: <topic_base>/<VIN>/v/<field>
MQTT_TOPIC_BASE=tesla

# =============================================================================
# Paths (relative to docker-compose.yml)
# =============================================================================
CERTS_PATH=./certs
KEYS_PATH=./keys
LOGS_PATH=./logs

# =============================================================================
# Fleet Telemetry Settings
# =============================================================================
# Log level: debug, info, warn, error
LOG_LEVEL=info
